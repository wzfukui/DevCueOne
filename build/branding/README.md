# Branding Asset Drop Zone

Place future app-branding assets here.

Current active assets:

- `icon.png`
- `icon.ico`
- `icon.icns`

Recommended filenames:

- `icon.png` for a master square PNG
- `icon.icns` for macOS packaging
- `icon.ico` for Windows packaging

Optional later assets:

- `dmg-background.png`
- installer or store preview images
